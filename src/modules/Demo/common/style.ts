import { StyleSheet } from 'react-native';

export const commonStyle = StyleSheet.create({
  container: {
    width: '100%',
    height: '100%',
    flexDirection: 'column',
    backgroundColor: '#fff',
    alignItems: 'center',
  },
});
